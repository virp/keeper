package command

import (
	"context"
	"fmt"
)

// Edit client command for updating item.
func (c *Command) Edit(ctx context.Context, name string) error {
	fmt.Println("Command not implemented yet")
	return nil
}
